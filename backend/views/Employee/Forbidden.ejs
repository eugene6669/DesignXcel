<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Access Denied - Design Excellence</title>

    <style>
        body {
            background: #fffbe6;
            color: #333;
            font-family: 'Segoe UI', Arial, sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
        }
        .forbidden-container {
            background: #fff8d1;
            border: 2px solid #ffd54f;
            border-radius: 16px;
            box-shadow: 0 4px 24px rgba(0,0,0,0.07);
            padding: 48px 36px 36px 36px;
            text-align: center;
            max-width: 400px;
        }
        .forbidden-icon {
            font-size: 54px;
            color: #ffc107;
            margin-bottom: 18px;
        }
        .forbidden-title {
            font-size: 2em;
            font-weight: bold;
            color: #b28704;
            margin-bottom: 10px;
        }
        .forbidden-message {
            font-size: 1.1em;
            margin-bottom: 28px;
        }
        .forbidden-btn {
            background: #ffc107;
            color: #333;
            border: none;
            border-radius: 6px;
            padding: 12px 32px;
            font-size: 1em;
            font-weight: 500;
            cursor: pointer;
            transition: background 0.2s;
            margin: 0 8px;
        }
        .forbidden-btn:hover {
            background: #ffb300;
        }
        .forbidden-btn.secondary {
            background: #6c757d;
            color: white;
        }
        .forbidden-btn.secondary:hover {
            background: #5a6268;
        }
    </style>
</head>
<body>
    <div class="forbidden-container">
        <div class="forbidden-icon">&#9888;</div>
        <div class="forbidden-title">Access Denied</div>
        <div class="forbidden-message">
            Sorry, you do not have permission to access this section.<br>
            Please contact your administrator if you believe this is a mistake.
        </div>
        <div>
            <button class="forbidden-btn" onclick="goToDashboard()">Go to Dashboard</button>
            <button class="forbidden-btn secondary" onclick="window.history.back()">Go Back</button>
        </div>
    </div>

    <script>
        function goToDashboard() {
            // Try to determine the user's role and redirect to appropriate dashboard
            const userRole = '<%= typeof user !== "undefined" && user.role ? user.role : "Guest" %>';
            
            switch(userRole) {
                case 'Admin':
                    window.location.href = '/Employee/AdminManager';
                    break;
                case 'InventoryManager':
                    window.location.href = '/Employee/InventoryManager';
                    break;
                case 'TransactionManager':
                    window.location.href = '/Employee/TransactionManager';
                    break;
                case 'UserManager':
                    window.location.href = '/Employee/UserManager';
                    break;
                case 'OrderSupport':
                    window.location.href = '/Employee/OrderSupport';
                    break;
                default:
                    window.location.href = '/login';
                    break;
            }
        }
    </script>

</body>
</html>
