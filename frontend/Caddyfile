# Caddyfile for DesignXcel Frontend
# Serves the React build as a static site

:80 {
    # Serve static files from build directory
    root * /app/build
    
    # Enable gzip compression
    encode gzip
    
    # Handle client-side routing (SPA)
    try_files {path} /index.html
    
    # Security headers
    header {
        # Enable HSTS
        Strict-Transport-Security "max-age=31536000; includeSubDomains"
        
        # Prevent clickjacking
        X-Frame-Options "SAMEORIGIN"
        
        # XSS protection
        X-Content-Type-Options "nosniff"
        X-XSS-Protection "1; mode=block"
        
        # Content Security Policy - Updated for Stripe blob URLs and network scripts
        Content-Security-Policy "default-src 'self' 'unsafe-inline' 'unsafe-eval' data: blob:; script-src 'self' 'unsafe-inline' 'unsafe-eval' data: blob: https://js.stripe.com https://hooks.stripe.com https://m.stripe.network https://ajax.googleapis.com; style-src 'self' 'unsafe-inline' data: blob:; img-src 'self' data: blob: https:; font-src 'self' data: blob:; connect-src 'self' data: blob: https://api.stripe.com https://hooks.stripe.com https://m.stripe.network https:; frame-src 'self' blob: https://js.stripe.com https://hooks.stripe.com https://m.stripe.network; worker-src 'self' blob:; child-src 'self' blob:;"
    }
    
    # Cache static assets
    @static path *.js *.css *.png *.jpg *.jpeg *.gif *.ico *.svg *.woff *.woff2 *.ttf *.eot
    header @static Cache-Control "public, max-age=31536000"
    
    # Log requests
    log {
        output stdout
        format console
    }
}
